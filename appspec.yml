version: 0.0 # 0.0으로 반드시 고정
os: linux
files:
  - source: /
    destination: home/ec2-user/app/step2/zip/
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user

hooks:
  ApplicationStart:
    - location: deploy.sh
      timeout: 60
      runas: ec2-user

# commit test 2020 11 26 22:41